safe job reservation (no duplicates)
ESS SoC check implementation. (and fallback)
retry counter.
automatic cleanup.
WOL trigger script.
Shutdown command / if no jobs is given.
full config at every job handshake. (to ensure always newest settings)
crash protection
no config file locally on slave, except server url. name for device is hostname/pc-name/user-name.
update script.
auto update script? (check newest version at every boot?)